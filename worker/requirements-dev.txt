# Development / test dependencies for the NavTrack worker
# Install with:  pip install -r requirements-dev.txt

# Runtime deps (pulled in transitively by worker.py)
-r requirements.txt

# Test framework
pytest>=9.0.3
pytest-cov>=7.1.0

# Mocking helpers
responses>=0.26.0          # HTTP mocking for requests-based calls
pytest-mock>=3.15.1        # provides the `mocker` fixture
