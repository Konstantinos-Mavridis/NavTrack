[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Ensure the worker root is on sys.path so `import worker` resolves
addopts = --import-mode=importlib
